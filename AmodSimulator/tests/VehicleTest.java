public class VehicleTest {

    //todo test what happens if a vehicle gets a request with same Origin as the vehicles position

    //todo test what happens if a vehicle can traverse more than one edge in a tick
}
